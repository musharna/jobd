- **`run_job` decomposed (pure refactor).** The worker's 585-line dispatch function — where the last three audits' worker bugs all lived — is now 148 lines of orchestration over seven early-return pre-flight helpers and a `_WorkloadRun` object holding the state its threads share (signal routing, SIGKILL escalation, the three watchdogs, drain hook, stdout streaming, grace wait, straggler reaping, final-state mapping). Same data, same lock discipline, no behavior change; the H-2 guaranteed-terminal `try/finally` shape is preserved exactly, and the three watchdog kill branches share one tail instead of three verbatim copies. One micro-alignment: the first-output-watchdog disarm now actually happens under `last_log_lock`, which is what its own comment always claimed.
